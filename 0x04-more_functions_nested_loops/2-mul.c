#include "main.h"

/**
 * mul - function to multiply two integers
 * @x:nthe first number to multiply
 * @y: the second number to multiply
 * Return: the multiplication result
 */

int mul(int x, int y)
{
	return (x * y);
}
